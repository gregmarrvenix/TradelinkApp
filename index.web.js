import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('TradeLinkApp', () => App);
AppRegistry.runApplication('TradeLinkApp', {
  rootTag: document.getElementById('root'),
});
